import { handleActions } from 'redux-actions';
import actions from '../actions/<%= reducer.reducerName %>';

const defaultState = {};
const { request, receive, error } = actions;

const <%= reducer.reducerName.toLowerCase() %> = handleActions(
  {
    [request]: (state) => {
      return { ...state, loading: true };
    },
    [receive]: (state, { payload: { data } }) => {
      return { ...state, data };
    },
    [error]: (state, { payload: { error } }) => {
      return { ...state, error };
    },
  },
  defaultState,
)

export default <%= reducer.reducerName.toLowerCase() %>;
